apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
spec:
  secretName: argocd-tls
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
  dnsNames:
    - argocd.home.lab
