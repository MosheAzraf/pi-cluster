apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-pgadmin
  namespace: argocd
spec:
  project: default

  sources:
    - repoURL: https://github.com/MosheAzraf/pi-cluster.git
      targetRevision: main
      path: kubernetes/apps/pgadmin/

  destination:
    server: https://kubernetes.default.svc
    namespace: pgadmin

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
