apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus
  namespace: monitoring
spec:
  secretName: prometheus-tls
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
  dnsNames:
    - prometheus.home.lab
