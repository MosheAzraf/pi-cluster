apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage
  namespace: homepage
spec:
  secretName: homepage-tls
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
  dnsNames:
    - homepage.home.lab
