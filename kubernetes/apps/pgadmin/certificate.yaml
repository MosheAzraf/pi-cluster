apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pgadmin
spec:
  secretName: pgadmin-tls
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
  dnsNames:
    - pgadmin.home.lab