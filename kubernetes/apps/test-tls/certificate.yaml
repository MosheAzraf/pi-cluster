apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whoami-cert
  namespace: default
spec:
  secretName: whoami-tls
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
  dnsNames:
    - whoami.home.lab
